# Python 兼容性规则

以下变化默认属于高风险或破坏性变化：

- 删除或重命名公共函数、类或方法。
- 改变必填参数的名称、位置、数量或默认值。
- 将同步接口改为异步接口，或反向修改。
- 改变导入路径、返回值形状、异常类型或副作用顺序。

以下变化通常风险较低，但仍需测试证明：

- 提取私有辅助函数，且公共入口保持不变。
- 消除内部重复代码，不改变分支和异常行为。
- 只调整局部命名、注释或内部数据结构。

AST 比较只能发现结构差异，不能证明运行时语义完全相同。因此，脚本报告是风险提示，不是测试的替代品。

